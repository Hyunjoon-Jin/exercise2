/**
 * 탈퇴를 확정하기 위해 사용자가 그대로 입력해야 하는 문구.
 *
 * 서버 액션 파일("use server")은 async 함수만 내보낼 수 있어 여기에 둔다.
 * 화면과 서버가 같은 문자열을 봐야 한다 — 한쪽만 고치면 아무도 탈퇴하지
 * 못하거나, 아무 글자나 쳐도 탈퇴가 된다.
 */
export const DELETE_CONFIRMATION = "계정을 삭제합니다";
